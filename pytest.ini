[pytest]
markers =
    integration: marks tests that require live network calls (like the HF Space)
    gemma: marks tests for the Gemma/Modal LLM integration (excluded from CI by default)
env =
    LANGCHAIN_TRACING_V2=false